/**
 * Copiá este archivo a config.js y completá los valores.
 * config.js está en .gitignore (podés commitear el ID del pixel si preferís).
 */
window.LANDING_CONFIG = {
  /** Meta Events Manager → Configuración del píxel → ID del píxel */
  META_PIXEL_ID: "",

  /** Tally → Publish → link del formulario (https://tally.so/r/xxxx) */
  TALLY_FORM_URL: "https://tally.so/r/REEMPLAZAR",

  /** Ruta de la página de gracias (mismo dominio que index.html) */
  THANK_YOU_PATH: "gracias.html",
};
